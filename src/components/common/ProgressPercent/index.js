import React from "react";
import classes from "./ProgressPercent.module.scss";

export default function ProgressPercent(props) {
  <div className={classes["progress-percent"]}>ProgressPercent</div>
}